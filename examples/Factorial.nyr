// NOTE: Since recursion is not working with the current interpreter, running this file will not work
def factorial(x) {
	if (x <= 1) {
		return 1;
	} else {
		return x * factorial(x - 1);
	}
}

let fac1 = factorial(1);
let fac10 = factorial(10);
